pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // This is key
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "MindiKott"
include(":app")
